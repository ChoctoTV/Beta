# ══════════════════════════════════════════════════════════════════════════════
#  ChoctoTV — Secret Configuration
#
#  Copy this file and rename it:
#    secret.txt
#
#  Fill in every blank below, drop it in the project root, and run ./start.sh
#  Run  ./start.sh verifyenv  to check for missing values before going live.
#  Run  ./start.sh resetenv   to generate a fresh copy from this template.
# ══════════════════════════════════════════════════════════════════════════════


# ── TWITCH ────────────────────────────────────────────────────────────────────

# Your channel name (lowercase, no #)
TWITCH_CHANNEL=

# Bot account username (can be your own account or a dedicated bot)
TWITCH_BOT_USERNAME=

# OAuth token for the bot account
# Generate at: https://twitchapps.com/tmi/
TWITCH_OAUTH_TOKEN=

# Twitch app Client ID
# Create an app at: https://dev.twitch.tv/console/apps
# Redirect URI must be set to: http://localhost
TWITCH_CLIENT_ID=

# Stream key — found at: https://dashboard.twitch.tv/settings/stream
TWITCH_STREAM_KEY=

# Twitch RTMP ingest — change only if your region has a faster ingest
TWITCH_RTMP_BASE=rtmp://live.twitch.tv/app


# ── STREAM ENCODING ───────────────────────────────────────────────────────────

STREAM_RESOLUTION=1280x720
STREAM_FPS=60
STREAM_BITRATE=4500k
STREAM_AUDIO_BITRATE=128k
STREAM_QUALITY=720p

# Buffer delay — match to your Twitch latency setting
# Low Latency:  3000   Normal: 10000   Local test: 0
STREAM_BUFFER_MS=3000


# ── SOLANA / NFT ──────────────────────────────────────────────────────────────

# Helius API key — used for NFT ownership verification via DAS API
# Free tier available at: https://helius.dev
HELIUS_API_KEY=


# ── PORTS ─────────────────────────────────────────────────────────────────────

API_PORT=3000
WS_PORT=3001
PUPCORE_PORT=3002
TELLER_PORT=3003


# ── ECONOMY ───────────────────────────────────────────────────────────────────

# Twitch emote name shown in cashout confirmation messages
CHOCTOPI_EMOTE=Choctopus

# Discord guild (server) ID — included in cashout and vcode webhook payloads
# Find it: Discord → right-click your server → Copy Server ID
DISCORD_GUILD_ID=




# ── TIPBOT WEBHOOK ────────────────────────────────────────────────────────────
# Both !cashout and !vcode <code> POST to the SAME webhook URL below.
# Tipbot routes based on payload shape: {user,user_id,amount} = cashout,
# {verify:{...}} = vcode verify.
TIPBOT_WEBHOOK_URL=http://choctopus.io:8888/ChoctoTvAPI
# Optional auth. Leave blank if endpoint doesn't need it.
TIPBOT_WEBHOOK_AUTH_TYPE=
TIPBOT_WEBHOOK_AUTH_TOKEN=
TIPBOT_WEBHOOK_AUTH_USER=
TIPBOT_WEBHOOK_AUTH_PASS=


# ── OVERLAY LAYERS (optional) ─────────────────────────────────────────────────
# Transparent Chrome windows layered on top of the stream.
# Use for alert boxes, tip notifications, Twitch extensions, etc.
# Each URL gets its own Chrome instance — they stack in order.
# Page must have: body { background: transparent; }

OVERLAY_URL_1=
OVERLAY_URL_2=
OVERLAY_URL_3=


# ── LOGGING ───────────────────────────────────────────────────────────────────

LOG_LEVEL=info
LOG_PRETTY=0       # Set to 1 for readable local dev output


# ── OPTIONAL ──────────────────────────────────────────────────────────────────

# Anthropic API key — used for pup name moderation (optional)
ANTHROPIC_API_KEY=


